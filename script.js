let cart = [];
h({ name: productName, prt() { let cartList = document.getElction updateCart() { let cartList = document.getElction updateCart() { let cartList = document.getElementById("cart("tomal-price"); cartList.innerHTML = ""; let tota("total-price"); cartList.innerHTML = ""; let totaice.textContent = `مجموع: ${total} تومان`;

                                                                                                                                 }

                              lPrice.textContent = `مجموع: ${total} تومان`;

                                                                               }

   lPrice.textContent = `مجموع: ${total} تومان`;

                             }

   lPrice.textContent = `مجموع: ${total} تومان`;

}

lPrice.textContent = `مجموع: ${total} تومان`;

}

